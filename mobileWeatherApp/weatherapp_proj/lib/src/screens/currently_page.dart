import 'package:flutter/material.dart';

class CurrentlyPage extends StatelessWidget {
  const CurrentlyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Currently Page'),
    );
  }
}
